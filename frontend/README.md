# Frontend — Divya Keerthi Academy

This is the web app for Divya Keerthi Academy. It needs the `server/`
folder running alongside it — see the top-level `README.md` (one folder
up) for full setup instructions.

Quick reference, if you've already done the full setup once:

```bash
npm install
npm run dev
```

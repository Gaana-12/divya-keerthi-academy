# Backend — Divya Keerthi Academy

This is the API and database for the Divya Keerthi Academy app. It needs
the `frontend/` folder running alongside it to be useful — see the
top-level `README.md` (one folder up) for full setup instructions.

Quick reference, if you've already done the full setup once:

```bash
npm install
npm start
```
